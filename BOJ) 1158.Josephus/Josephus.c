#include <stdio.h>

typedef struct ListNode {
    int val; 
    struct ListNode* next;
} node;

int main(void)
{
    scanf("%i,%i", &N, &K);
}

//N개만큼의 노드로 이어진 리스트 생성
node list(int N, int K)
{
    node* head = (node*)malloc(sizeof(node)); 
    node* curr = head
    node* tmp = (node*)malloc(sizeof(node));
    for (i = 1; i < N; i++)
    {
        
    }
}

void deletion()